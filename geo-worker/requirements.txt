rasterio>=1.3.9
numpy>=1.26.0
Pillow>=10.0.0
boto3>=1.34.0
redis>=5.0.0
sqlalchemy>=2.0.30
psycopg[binary]>=3.1.18
pydantic-settings>=2.2.0
shapely>=2.0.0
pytest>=8.0.0
# NOTE: the shared package `wss_common` is installed separately by the
# Dockerfile (`pip install /app/packages/wss_common`) and, for local dev, with
# `pip install -e packages/wss_common` from the repository root.
